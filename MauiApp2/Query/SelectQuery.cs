namespace MauiApp2.Query
{
    public static class SelectQuery
    {
        // Строка Mysql запроса для выборки всех счётчиков
        public static string SelectAllDevice = "SELECT * FROM `device`";
    }
}
